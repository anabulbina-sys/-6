def area(a, b, h):
    """Площадь трапеции: S = (a + b) * h / 2"""
    return (a + b) * h / 2

def inscribed_circle_radius(a, b, h):
    """
    Радиус вписанной окружности трапеции.
    Окружность можно вписать только если a + b = c + d (сумма оснований = сумма боковых сторон)
    и высота h = 2r, значит r = h / 2.
    """
    return h / 2  # Упрощённо, для равнобедренной трапеции с вписанной окружностью

def circumscribed_circle_radius(a, b, h):
    """Для трапеции описанная окружность существует не всегда. Возвращаем None"""
    return None